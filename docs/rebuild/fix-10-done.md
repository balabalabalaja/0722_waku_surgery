# fix-10-done

**fix-10 完成**（玩家第 35 轮，含"半透明的白色"补充确认）：

1. **窗口框纯白化**：三个窗口的框线 + 45° 角标去掉暗色晕
   （`drawBracket` 内 shadow 移除）——纯白线；其余元素阴影不动
   （环棱线暗晕、环落影、素材落脸投影、快门/顶栏投影均保持原样）。
2. **顶栏白色简洁风**：灰玻璃 → **白色半透明磨砂药丸**
   （`bg-white/65 + backdrop-blur`、白描边、轻投影）；图标线稿、
   SAT 标签/轨道/滑钮/百分比全部翻深色（neutral-800/600/500 +
   black/10 边），对比度保住，气质干净轻盈。

取证指针：

- `ui-previews/01-opening-dressed.png` 已刷新（白磨砂顶栏 + 深色
  图标 + 纯白窗口框同屏可验）；02/03 同步刷新
- 回归：probe-fix07 全量复跑 **0 失败**（顶栏几何/命中断言不受
  配色影响，实证）；tsc + 19 单测 + 生产构建全绿
- dev server tmux `surgery-preview`:8891
